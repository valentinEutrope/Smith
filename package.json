{
  "name": "smith",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:8080",
  "scripts": {
    "start": "cd src/client && concurrently \"npm run start\" \"wait-on http://localhost:3000 && npm run electron\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^6.0.1",
    "jake": "^10.8.2",
    "jake-synchronoustasks": "^1.0.0"
  },
  "dependencies": {
    "wait-on": "^6.0.0"
  }
}

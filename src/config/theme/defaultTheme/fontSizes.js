import { baseFontSize } from "./variables";

export const fontSizes = {
  base: baseFontSize,
  paragraph: baseFontSize * 1.125,
  header: {
    big: baseFontSize * 2.5,
    medium: baseFontSize * 2,
    small: baseFontSize * 1.75,
  },
};

import { lineHeight } from "./variables";

export const spaces = {
  none: 0,
  xs: lineHeight / 2,
  sm: lineHeight,
  md: lineHeight * 2,
  lg: lineHeight * 4,
  xl: lineHeight * 8,
  xxl: lineHeight * 12,
};
